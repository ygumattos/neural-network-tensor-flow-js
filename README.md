# neural-network-tensor-flow-js
Neural network user classification using TensorFlow.js in Node.js. This project demonstrates how to train and use a simple neural network to classify users into subscription tiers (premium, medium, basic) based on demographic and categorical features.
